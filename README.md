# CoolifyHello

A deliberately small, public deployment demo for the ChatArch Coolify platform.

It proves this path with a real application:

```text
repository -> Git push -> Coolify build -> automatic cool domain -> public HTTPS URL
```

The image is built from `Dockerfile`, serves a page on port `80`, and exposes a JSON health endpoint at `/health`.
